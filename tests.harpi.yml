variables:
  baseAddress: "https://localhost:7076"
  autoGeneratedId: $(guid) 

requests:
  - name: "create data"
    method: "post"
    url: "$(baseAddress)/api/students"
    jsonBody: 
      id: "$(autoGeneratedId)"
      cprnumber: "12345"
      email: "HarpiTestEmail@mail.com"
      name: "TestNavnHarpi"
      campus: "Odense"
      gender: 0

    asserts:
      statusCodeEquals: 201
